import { AltPressedDirective } from './alt-pressed.directive';

describe('AltPressedDirective', () => {
  it('should create an instance', () => {
    const directive = new AltPressedDirective();
    expect(directive).toBeTruthy();
  });
});
